# Codex 交接：工作台已整理，下一步跑新故事端到端验证

当前日期：2026-05-12  
当前主工程目录：`/Volumes/语苗计划/外置硬盘/New project`

## 当前状态

我们已经完成了两轮工程整理：

1. 用 `邯郸学步` 作为回归样例，确认完整流程已经跑通过。
2. 做了工程体检和工作台体验优化，准备进入“新故事验证”阶段。

接下来不要继续在本上下文里打磨旧问题。请在新窗口/新上下文里，从一个全新故事开始跑工作台 19 步流程，并在真实操作中发现问题再修。

## 启动工作台

```bash
cd "/Volumes/语苗计划/外置硬盘/New project"
/opt/miniconda3/bin/python3 workbench_app.py
```

也可以双击：

```text
/Volumes/语苗计划/外置硬盘/New project/run_workbench.command
```

## 刚完成的关键优化

### 工程体检

- `story_workflow.py` 新增 `doctor-project`。
- 工作台新增 `⑲ 工程体检`。
- 体检报告写入单个故事项目：

```text
99_项目状态/doctor_project_report.md
```

### 工作台界面

- 主界面只保留“故事名”，`Slug` 和“短名”移入高级设置，默认自动生成。
- 填中文故事名后，会自动推导桌面项目路径：

```text
~/Desktop/故事剪辑：故事名
```

- 点击 `保存并识别` 会自动建立标准目录，不需要手动先建文件夹。
- 顶部重复的“选择故事文件夹”已移除，保留下方“桌面项目”的选择入口。
- 故事类型更新为：
  - 寓言故事
  - 成语故事
  - 童话故事
  - 民间故事
  - 神话故事
  - 红色故事
  - 历史故事
  - 科普故事
- 适龄段更新为：
  - 3-5岁
  - 6-8岁
  - 9-11岁
  - 12-14岁
  - 15岁以上
- 时长从自由文本改为“分 / 秒”两个框；也可以不填，后续尽量由成片时长自动回填。
- `交付后更新集数` 和 `本期不占集数` 已做双向互斥。
- `高级设置（通常不用管）` 改成 `高级设置`。
- 主界面的 `停止提醒` 按钮移除；任务完成/失败后的提醒弹窗里有停止提醒按钮。

### 故事原文与分镜意图

- 右侧文本框现在仍用于保存故事原文。
- 如果用户在文本框中手动换行，第 2 步生成 Codex 出图任务时：
  - 每个非空行会先写入分镜文本文件。
  - 任务书会明确告诉 Codex：优先把这些换行当作候选镜头/分镜边界。
- 也就是说，换行不再只是普通文本格式，而是用户的“预切分镜头意图”。

### 运行状态与计时

- 工作台新增“运行状态”区域，显示当前任务和已用时。
- 生成视频片段时会尽量显示 `已生成/总数`。
- 完成或失败后会持续响系统提示音，直到用户在弹窗中点击停止。
- 2026-05-12 补充：完成/失败现在只弹一个“需要处理”提醒窗，不再同时弹系统 OK 框；提醒声使用 macOS 系统音循环播放，直到点击“我知道了，停止提醒”。
- 每一步运行时间会记录两份：

单个故事项目：

```text
99_项目状态/workbench_run_history.json
```

整个工作台统一统计：

```text
/Volumes/语苗计划/外置硬盘/New project/output/workbench_metrics/workbench_run_history.jsonl
```

## 新故事验证建议

新窗口接手后，建议这样开始：

1. 启动工作台。
2. 在“故事名”里输入新故事中文名。
3. 确认“桌面项目”自动变为：

```text
/Users/baiyanglin/Desktop/故事剪辑：新故事名
```

4. 选择故事类型、适龄段。
5. 粘贴故事原文；如果已有镜头切分意图，用换行分隔。
6. 点击 `① 保存并识别`。
7. 点击 `② 生成给Codex的出图任务`，让 Codex 按任务书生成/确认分镜和图片。
8. 继续按 ③ 到 ⑲ 真实跑完整链路。

如果中途出现 bug，优先修：

- 工作台路径是否串到旧项目。
- `project_manifest.json` 是否正确记录输入、输出、QA。
- 当前步骤失败后能否从该步骤继续。
- 日志是否告诉用户下一步该做什么。
- 计时和完成提醒是否符合实际使用。

## 重要文件

- 工作台入口：`/Volumes/语苗计划/外置硬盘/New project/workbench_app.py`
- 统一 CLI：`/Volumes/语苗计划/外置硬盘/New project/story_workflow.py`
- 项目/manifest/QA：`/Volumes/语苗计划/外置硬盘/New project/story_project.py`
- 全局配置：`/Volumes/语苗计划/外置硬盘/New project/pipeline_config.json`
- 流程规范：`/Volumes/语苗计划/外置硬盘/New project/STORY_PIPELINE.md`
- 二层体检记录：`/Volumes/语苗计划/外置硬盘/New project/ENGINEERING_AUDIT_20260512.md`

## 给新窗口 Codex 的第一句话

可以直接这样发：

```text
请先阅读 `/Volumes/语苗计划/外置硬盘/New project/NEXT_CODEX_PROMPT_20260512_NEW_STORY_VALIDATION.md`。
我们已经完成 Story Video Composer 工作台的工程整理和界面优化。
现在请不要继续打磨旧上下文，请启动工作台，用一个全新故事从第 ① 步开始做端到端验证；过程中发现路径、manifest、状态提示、计时、提醒或具体步骤 bug，再就地修复。
```
