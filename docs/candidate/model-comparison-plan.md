# 后续模型对照方案（本轮不执行）

固定同一候选提交、同一输入文件SHA、相同确认文本/供应商参数/交付合同与独立审核规则。为每个待比较的窗口设置建立隔离新项目，分别记录首轮质量分数、关键错误、有效/无效返工、总墙钟及等待、生成/审核/编码次数、能取得的模型与推理设置、Token和供应商请求ID。使用相同审核范围；模型实际设置取不到时写null，将界面期望单列，不能把期望当证明。

先比较质量是否达标与返工，再比较实测耗时和可取得的额度数据。不使用社区传闻预设高低推理档更省；主任务和子任务设置保持各实验约定，禁止中途隐式切换。当前候选没有预装模型路由。

账号使用率属于共享账号，各任务可能重叠；重置窗口/重置卡会改变可比性。只有能独立归因的测量才作对照；未知项保留null。单次结果不推断长期首轮成功率；离线确定性耗时/少生成一张图/少准备一个审核包不能换算成ChatGPT周额度节省百分比。
