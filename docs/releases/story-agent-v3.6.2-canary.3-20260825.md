# Story Agent v3.6.2-canary.3（2026-08-25）

## 七色花二次实测修正

- 修复尾声叙事中的“不能跑跳”被误判为独立寓意；只有明确面向听众或总结教训的文本才进入寓意卡流程。
- 语义分类器与适配器绑定升级为 v2，旧分类结果不会被当成当前结果复用。
- Runtime 会移除因分类修正而不再适用、且由 Runtime 自己生成的默认语义映射；不会删除模型或用户明确选择的映射。
- 故事正文只能证明内容存在，不能为跨产品路由策略提供高优先级来源；这类复合策略由 Runtime 确定性降为 `agent_inference`。
- 单一 Logo 路径不能同时证明 `allowed_uses` 与 `max_per_frame`；若配置没有提供完整复合策略，资产使用策略同样诚实降级。
- Runtime 规范化会归档旧合同哈希、审核和锁，再使用新哈希做一次独立审核；不会重写整份合同或进入自动循环。

## 验证

- 完整测试：`557` 项通过。
- 七色花回归覆盖：结局仍属 `story_body`、错误 moral 默认映射被移除、正文路由与 Logo 复合策略来源被规范化。
