# Story Agent v3.6.2-canary.4（2026-08-25）

## 审核协议兼容与结果复用

- 合同审核矩阵同时接受旧式 `evidence` 与更完整的 `conclusion + contract_paths + trusted_evidence` 结构。
- `review_boundary` 等辅助审计行可以保留，但不能冒充七个必需合同节；七节覆盖要求不变。
- 当前合同、说明、可信来源与审核包哈希均未变化时，Runtime 直接复用已通过的独立审核并生成锁，不重复调用大模型。
- 低于 85 分、含关键错误、证据不完整或哈希不一致的审核仍不可复用。

## 验证

- 完整测试：`558` 项通过。
- 七色花 96 分审核的结构化证据已由机器门禁接受，无需为字段形态差异重新审核。
