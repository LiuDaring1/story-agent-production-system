# Story Agent v3.6.2-canary.6（2026-08-25）

## 多图审核丰富证据结构兼容

- 逐图 `evidence_matrix` 接受 `conclusions[]` 作为有内容的结构化证据，不再要求模型把同一结论改写成单个 `evidence` 字符串。
- `scale_readability` 可由全部通过且各自带证据的 `relationships[]` 证明，不要求额外重复一段顶层尺度说明。
- 兼容条件仍要求对应项目通过、证据非空并覆盖计划中的全部必需维度；缺失、失败或无证据的关系不能借此放行。
- 七色花当前 94 分视觉小样审核可直接复用，本次修复不重新调用审核模型或 ImageGen。

## 验证

- 七色花真实审核经机器门禁验证为零问题。
- 完整测试：`561` 项通过。
