# 绵羊姐姐主账号发布视频复盘与复用规范

本文件沉淀《自相矛盾》主账号发布视频从测试到可发布版的经验。目标不是只复现本期，而是让之后每一期都能按同一套判断标准快速跑通：先小样验证，再高规格成片。

## 本期结论

最终可发布版本：

`/Users/baiyanglin/Documents/New project/output/zixiangmaodun_release_publish/main_full_release/high_2160x2880_v3/主账号发布视频.mp4`

最终规格：

- 画布：2160x2880，3:4。
- 中间 16:9 区域：2160x1216，嵌入 3:4 底板中央。
- 时长：128.73 秒。
- 视频：H.264，25fps，CRF 17。
- 音频：人声 + 背景视频音乐混合，AAC 双声道。
- 字幕：独立叠在整个 16:9 横屏区域底部，不烧进故事框内视频。
- A/B 景：A 景为人物 + 小故事框；B 景为无人物大故事框。B 景窗口使用 `30-52,82-104`。

## 复用总流程

1. 素材准备

   - 故事背景视频：无字幕、有音乐版本。
   - 人像绿幕视频：建议先在剪映或同类工具里完成色彩还原、调色、美颜，再交给脚本抠像。
   - 字幕：SRT 文件，用于最后叠在 16:9 区域底部。
   - 底板图：上下两段生成后拼合，中间必须保留精准 16:9 区域。
   - 故事框：透明 PNG，主题元素随故事变化，但外轮廓必须完整留在画布安全区内。
   - LOGO：使用原版已抠好的 PNG，不要重新抠。

2. 先跑检查版

   每个新故事建议先跑 10-20 秒检查版，看四件事：

   - 人像抠像是否干净，尤其头发、衣服边缘、手指。
   - 人物大小和位置是否符合右侧锚点。
   - A/B 景是否切换正常，没有残影或叠图。
   - 字幕和 LOGO 是否固定在 16:9 区域，而不是跟故事框走。

3. 抽帧检查

   不要直接相信整片渲染。至少抽这些点：

   - A 景站立稳定点：例如 10-20 秒。
   - A 景大手势点：找手伸得最远的地方。
   - B 景中段：检查大框是否居中、字幕是否压框。
   - A/B 切换后 1-2 秒：检查是否有残影。

4. 高规格输出

   检查版确认后再跑完整版。高规格使用 `--output-scale 2`，让脚本原生合成 2160x2880，而不是先做 1080x1440 再拉伸。

## 本期固定参数

这些参数是《自相矛盾》最终 v3 的可发布参数，可作为下一期默认起点。

```bash
--output-scale 2
--video-box '0,360,1080,608'
--story-box '210,270,910,512'
--b-story-box '356,180,1209,680'
--b-windows '30-52,82-104'
--story-logo-width-a 220
--story-logo-x 38
--story-logo-y 45
--subtitle-font-size 46
--subtitle-margin-v 18
--person-crop '0,0,2160,3150'
--person-height 1380
--person-x 970
--person-y -300
--keyer colorkey
--chroma-color 0x268a3c
--chroma-similarity 0.14
--chroma-blend 0.015
--crf 17
--preset medium
```

坐标说明：

- `--video-box` 是 1080x1440 竖版基准坐标，脚本会按 `--output-scale` 放大。
- `--story-box`、`--b-story-box`、LOGO 和字幕参数是 1920x1080 横屏基准坐标，脚本同样会按倍率放大。
- `--person-crop` 是人像源素材坐标，不按输出倍率缩放。它依赖原始绿幕视频分辨率。

## 抠像经验

这次最主要的坑是：不是脚本不能抠，而是一开始没有先用代表性帧调参数。

正确做法：

1. 先抽人像源的代表帧

   - 正面站立帧。
   - 手伸出画面最远的帧。
   - 头发边缘最容易出问题的帧。

2. 只裁上下，尽量不裁左右

   本期错误版为了去掉底部白线，把左右也裁了，导致手被脚本裁断。后面修正为：

   ```bash
   --person-crop '0,0,2160,3150'
   ```

   意思是保留整宽，只裁掉底部地面和白线。

3. 参数从保守开始

   本期最终参数：

   ```bash
   --chroma-color 0x268a3c
   --chroma-similarity 0.14
   --chroma-blend 0.015
   ```

   判断规则：

   - 如果头发被吃掉、发顶变白或断裂：降低 `similarity`。
   - 如果边缘还有明显绿色：略微提高 `similarity`，或增加 despill。
   - 如果边缘太硬：略微提高 `blend`。
   - 如果衣服浅色被误抠：降低 `similarity`，不要强抠。

4. Mask 处理顺序

   脚本里这次稳定下来的逻辑是：

   - `colorkey` 先生成透明度。
   - `alphaextract` 得到 mask。
   - `erosion,dilation` 先收边再恢复，减少绿边和脏边。
   - `despill=type=green:mix=0.35` 去绿色溢色。

## 人物位置规范

本期最终可接受的位置：

```bash
--person-height 1380
--person-x 970
--person-y -300
```

判断标准：

- 人物在 16:9 区域右侧，但不要贴边。
- 头顶不要留太多空，也不要顶到上边。
- 身体底部可以被 16:9 区域底部自然截掉，但不能露出绿幕源里的地面或白线。
- 人物层必须在故事框和背景视频之上，手势不能被故事框遮挡。
- 不要为了适配画面裁左右，除非源素材本身左右有明显无效区域。

以后如果源视频改为横拍，仍然按结果检查：

- 先保留人物完整手势范围。
- 根据人物实际包围盒定 `person-crop`。
- 仍以右侧锚点和人物高度为主，不固定死源素材的裁剪坐标。

## A/B 景规范

A 景：

- 人物 + 背景大底图 + 小故事框。
- 小故事框使用 `--story-box '210,270,910,512'`。
- 人物层在最上方。

B 景：

- 不出现人物。
- 大故事框居中展示背景视频。
- 大故事框使用 `--b-story-box '356,180,1209,680'`。

切换频率：

- 正片大约每 20-30 秒切一次，不要几秒钟频繁切。
- 在语义或动作比较自然的位置切。
- 2-3 分钟故事通常出现 2-4 段 B 景即可。

重要实现规则：

- A/B 不能用“把 B 透明层 overlay 到 A 上”的方式做切换。
- 必须先分别合成完整 A 画面和完整 B 画面，再按时间窗口整帧切换。
- 本期 1:32 的残影错误就是因为旧逻辑叠加透明层，导致 A 景残留在 B 景上。

## LOGO 和字幕规范

LOGO：

- 使用原始已抠 PNG：`标题初级版本.png`。
- 固定在整个 16:9 横屏区域左上角。
- 不绑定故事框，不随 A/B 景移动。
- 不重新抠，不重新压缩后再抠。

字幕：

- 从 SRT 独立生成透明字幕层。
- 固定叠在整个 16:9 横屏区域底部。
- 不放进故事框内。
- A 景和 B 景位置一致。

本期参数：

```bash
--subtitle-font-size 46
--subtitle-margin-v 18
```

## 底板图规范

底板图生成经验：

- 3:4 竖版画布，中间精准留出完整 16:9 区域。
- 上下内容分别生成更稳定，再拼合。
- 中间区域必须是可被视频完全覆盖的空区，不要让生成模型把装饰、文字、图标伸进去。
- 生成出来后脚本会对中间视频区域做透明挖空，防止底板元素压住 16:9 视频。

本期稳定坐标：

```bash
--video-box '0,360,1080,608'
```

高规格时对应实际像素：

```text
x=0, y=720, width=2160, height=1216
```

## 故事框规范

故事框可以每期根据故事生成新样式，但必须遵守：

- PNG 带 alpha。
- 外轮廓完整在 1920x1080 画布内，四周留安全边距。
- 中间视频窗口比例接近 16:9。
- 主题元素只作为边框装饰，不要遮挡视频主体。
- A 框和 B 框可以同风格不同尺寸，但要来自同一视觉体系。

生成后必须抽帧检查：

- 四角是否被切掉。
- 是否存在生硬裁边。
- 是否有多余背景没抠干净。
- 大 B 框是否给左上角 LOGO 留出空间。

## 输出质量规范

默认高规格输出：

```bash
--output-scale 2
--crf 17
--preset medium
```

判断：

- 如果背景视频只有 1080p，放到 2160x2880 后会有一定放大，但可接受。
- 人像源如果是 4K 竖屏，最终人物质量主要取决于源视频和抠像。
- 不建议把 1080x1440 小成片再拉伸，应直接用 `--output-scale 2` 原生合成。

## 推荐下次执行清单

每个新故事开始前，准备这些路径：

- 故事背景视频：无字幕、有音乐。
- SRT 字幕。
- 人像绿幕视频：已调色、美颜。
- 3:4 底板图。
- A/B 故事框 PNG。
- LOGO PNG。

执行顺序：

1. `ffprobe` 检查所有视频时长、分辨率、音频。
2. 抽人像源 2-3 帧，确认地面/白线位置和手势范围。
3. 做一张单帧抠像测试，先看头发、手、衣服边缘。
4. 跑 10-20 秒检查版。
5. 抽 A 景、B 景、切换点、手势点。
6. 用户确认后跑完整版高规格。
7. 最终再抽至少 3 帧，并确认音频和分辨率。

## 这次踩坑总结

- 不要用未调色、未美颜的人像源直接进入最终流程；色彩还原和美颜应在剪映等工具中先处理。
- 不要自动裁左右；手部动作的范围比静态人像包围盒更重要。
- 不要只看开头帧；中后段的大手势、表情和头发边缘更容易暴露问题。
- 不要把字幕放进故事框；字幕属于整个 16:9 横屏画面。
- 不要把 LOGO 绑定到故事框；LOGO 属于整条横屏视频。
- 不要用透明 overlay 做 A/B 切换；要整帧切换。
- 不要等完整渲染完才发现抠像问题；单帧测试可以节省大量时间。

